import React from 'react'

function Status({options}) {
  return (
    <div>
         <select value={selected} onChange={handleChange}>
        
          <option  value={item.value}>Not Coplete
          complete
          </option>
        
           
          </option>
        
      </select>
    </div>
  )
}

export default Status